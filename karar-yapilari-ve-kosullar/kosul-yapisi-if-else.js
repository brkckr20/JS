/* kullanıcı bilgisi varsa ekrana yazdir. */

let username = "";

if (username.length > 0) { /* if kısmı her zaman true ise çalışır */
    console.log(username)
} else {
    console.log("Kullanıcı adi boş");
}